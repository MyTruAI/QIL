from qil.utils import normalize_data, encode_state, calculate_reward

# Example usage
data = [5, 10, 15, 20]
normalized_data = normalize_data(data)

state = [1, 0, 1]
encoded_state = encode_state(state)

reward = calculate_reward(0.5, 0.8)
